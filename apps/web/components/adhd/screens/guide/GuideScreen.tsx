'use client';

import { MCPGuideView } from './components';

/**
 * Claude 연결하기 화면
 * screens/ 아키텍처에서 사용하는 독립적인 화면 컴포넌트
 *
 * MCPGuideView를 직접 사용
 */
export function GuideScreen() {
  return <MCPGuideView />;
}
