'use client';

/**
 * AppLifecycleHandler (Web Stub)
 *
 * Capacitor 앱 라이프사이클 이벤트 처리는 React Native 앱으로 이전되었습니다.
 * 웹/Electron 환경에서는 아무것도 렌더링하지 않습니다.
 */
export function AppLifecycleHandler() {
  return null;
}
