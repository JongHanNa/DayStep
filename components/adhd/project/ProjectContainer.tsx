'use client';

/**
 * Project Container
 *
 * 기존 ProjectMode를 Container 패턴으로 re-export합니다.
 */

export { default } from '../ProjectMode';
export { default as ProjectContainer } from '../ProjectMode';
