'use client';

import { GuideView } from '@/components/adhd/project/GuideView';

/**
 * 사용법 배우기 화면
 * screens/ 아키텍처에서 사용하는 독립적인 화면 컴포넌트
 */
export function GuideScreen() {
  return <GuideView />;
}

export default GuideScreen;
